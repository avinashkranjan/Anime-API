# Example .env file for Redis configuration
# Copy this to .env and update with your actual Redis credentials

# Server Configuration
PORT=3000

# Redis Caching Configuration
REDIS_ENABLED=true
REDIS_URL=redis://default:u3ItFdatpfEvRa8nMro9tFEamJv3bIo6@redis-17862.fcrce171.ap-south-1-1.ec2.cloud.redislabs.com:17862
REDIS_MAX_RECONNECT_ATTEMPTS=50

# Note: Configure these on your Redis server (not in .env):
# - maxmemory 10gb
# - maxmemory-policy allkeys-lru

# Node Cache Configuration (fallback when Redis is disabled or fails)
NODE_CACHE_TTL=86400
NODE_CACHE_CHECK_PERIOD=600
NODE_CACHE_MAX_KEYS=1000
